services:
  app:
    container_name: palmer-devops
    build: .
    ports:
      - "3000:3000"
    restart: always
    environment:
      NODE_ENV: production

# Optional: define a custom network for isolation
networks:
  app-network:
    driver: bridge